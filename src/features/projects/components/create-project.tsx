"use client";

import { Button } from "@/components/ui/button";
import { PlusIcon } from "lucide-react";

export const CreateProject = () => {
  return (
    <Button>
      <PlusIcon />
      Create Project
    </Button>
  );
};
